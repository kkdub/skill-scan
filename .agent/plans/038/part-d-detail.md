# Part d: Depth-3 getattr on tracked refs and bare call resolution

JIT part detail generated by `/run` before delegating to build-and-test. This is the FINAL FEATURE PART — includes acceptance scenarios.

## Implementation Steps

1. **Step**: Add getattr-on-ref and bare-call detection to `_ast_dynamic_exec_detector.py`
   - **Files**: `src/skill_scan/_ast_dynamic_exec_detector.py`
   - **Notes**: Two new detection paths in the existing walk loop:

     PATH 1 — getattr on tracked module ref: When the Call is `getattr(m, 'attr')` and `m` resolves in ref_table as a module ref:
     - If 2nd arg is a Constant string: check if attr is dangerous (using `_is_dangerous_ref_attr`). If yes, store result as func_ref in ref_table AND emit EXEC-002. If not dangerous, still store as func_ref (enables depth-3 tracking for non-dangerous intermediate lookups).
     - If 2nd arg is a Name that resolves in symbol_table to a string: check same way.
     - This path currently does NOT exist — `_check_getattr_call` only handles symbol_table resolution, not ref_table resolution.

     PATH 2 — Bare call on tracked func_ref: When the Call is `Call(func=Name)` and `Name.id` resolves in ref_table as func_ref: check if `resolved` is dangerous (attr part is in `_EXEC_ATTR_NAMES` or full qualified name is in `_DANGEROUS_QUALIFIED`). If yes, emit EXEC-002 CRITICAL.

   File is at 233/350 (117 lines headroom).

2. **Step**: Ensure all existing tests still pass
   - **Files**: `tests/unit/test_dynamic_exec_detector.py`, `tests/unit/test_dynamic_exec_scanner.py`
   - **Notes**: Verify no regressions. The new EXEC-002 findings are additive.

## Refined Criteria

- `f = getattr(os_mod, 'system'); f('cmd')` where `os_mod` is tracked module emits EXEC-002
- `b = __import__('builtins'); e = getattr(b, 'eval'); e('code')` emits EXEC-002
- `getattr(tracked_mod, 'path')` where 'path' is not dangerous produces no EXEC-002
- Full depth-3 chain produces both EXEC-006 (for __import__) and EXEC-002 (for resolved call)

## Dependencies Discovered

- `_ref_lookup` in `_ast_dynamic_exec_detector.py` (line 39): scope-aware ref_table lookup — reuse for all ref_table lookups
- `_is_dangerous_ref_attr` in `_ast_dynamic_exec_detector.py` (line 109): checks module.attr against _EXEC_ATTR_NAMES and _DANGEROUS_QUALIFIED
- `_track_func_ref` in `_ast_dynamic_exec_detector.py` (line 138): stores func_ref entries — Part D adds a similar track from getattr result
- `RefEntry(kind='func_ref', resolved='module.attr')` pattern established in Part C
- The walk loop processes ast.Assign first (tracking), then ast.Call (detection) — Part D needs both getattr-on-ref (Call) and bare-call-on-funcref (Call)
- `_DANGEROUS_QUALIFIED` frozenset (line 36): combined _UNSAFE_EXEC_CALLS | _SUBPROCESS_CALLS
