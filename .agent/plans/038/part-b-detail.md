# Part b: ref_table pre-pass infrastructure

JIT part detail generated by `/run` before delegating to build-and-test.

## Implementation Steps

1. **Step**: Create `_ast_ref_tracker.py` with `RefEntry` dataclass and `build_ref_table` function
   - **Files**: `src/skill_scan/_ast_ref_tracker.py`
   - **Notes**: `RefEntry`: frozen dataclass with `slots=True`, fields `kind` (`Literal['module', 'func_ref']`) and `resolved` (`str`, e.g. `'os'`, `'os.system'`). `build_ref_table(tree, alias_map)` walks assignments tracking `x = __import__('mod')` and `x = importlib.import_module('mod')`. Use `_build_scope_map` from `_ast_split_detector.py` for scope-aware keys (`funcname.varname` format). Check inner call via `get_call_name` from `_ast_imports.py`. Extract module name from first `Constant` arg. Import names to recognize: `"__import__"`, `"importlib.import_module"`, `"builtins.__import__"`, `"__builtins__.__import__"` (the latter two were added in Part A). ~80-120 lines; new file has full 350-line budget.

2. **Step**: Wire `build_ref_table` into `analyze_python`
   - **Files**: `src/skill_scan/ast_analyzer.py`
   - **Notes**: Call `build_ref_table(tree, alias_map)` after `symbol_table` build (around line 44). Import and re-export. For now, just build it — Parts C and D will pass it to `detect_dynamic_exec`. `ast_analyzer.py` at ~141/350, ample room.

## Refined Criteria

- `build_ref_table` function exists in `_ast_ref_tracker.py` and returns `dict[str, RefEntry]`
- `m = __import__('os')` at module level produces ref_table entry with key `'m'` and `resolved='os'`
- `mod = importlib.import_module('subprocess')` produces ref_table entry with `resolved='subprocess'`
- Function-scoped `m = __import__('os')` inside `def foo()` uses key `'foo.m'`
- `build_symbol_table` return type remains `dict[str, str]` (not modified)

## Dependencies Discovered

- `_build_scope_map(tree)` in `_ast_split_detector.py:121`: maps `id(node) -> scope_name` — reuse for scope-aware ref_table keys
- `get_call_name(node, alias_map)` in `_ast_imports.py`: resolves call names; recognizes `__import__`, `importlib.import_module`, `builtins.__import__`, `__builtins__.__import__`
- `build_symbol_table(tree)` in `_ast_symbol_table.py`: returns `dict[str, str]` — must NOT be modified
- `_IMPORT_CALL_NAMES` frozenset in `_ast_detectors.py` (added in Part A): `{"__import__", "importlib.import_module", "builtins.__import__", "__builtins__.__import__"}` — could reuse or define own set
