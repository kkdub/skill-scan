# Part c: Depth-2 attribute access on tracked module refs

JIT part detail generated by `/run` before delegating to build-and-test.

## Implementation Steps

1. **Step**: Add ref_table-aware detection to `_ast_dynamic_exec_detector.py`
   - **Files**: `src/skill_scan/_ast_dynamic_exec_detector.py`
   - **Notes**: Extend `detect_dynamic_exec` (or add a new detection loop in the same function) to:
     - Walk `Call(func=Attribute(value=Name))` nodes where `Name.id` resolves in `ref_table` (scope-aware lookup). If ref_table entry is a module and `attr` is dangerous (in `_DANGEROUS_NAMES` minus `getattr`/`__import__`, or fully-qualified `module.attr` is in `_UNSAFE_EXEC_CALLS`), emit EXEC-002 CRITICAL.
     - Walk `Assign` nodes where RHS is `Attribute(value=Name)` and `Name` resolves in `ref_table` as module — store target var as `RefEntry(kind='func_ref', resolved='module.attr')` in `ref_table`.
     - For ref_table scope-aware lookup: can't reuse `_scoped_lookup` directly (typed for `dict[str, str]`). Write a small `_ref_lookup` helper or do inline `scope.name` then bare `name` lookups.
     - File at 124/350, 226 lines of headroom — plenty of room.

2. **Step**: Thread `ref_table` parameter through `analyze_python` to the detector
   - **Files**: `src/skill_scan/ast_analyzer.py`, `src/skill_scan/_ast_dynamic_exec_detector.py`
   - **Notes**: Add `ref_table` kwarg to `detect_dynamic_exec`. In `analyze_python`, re-add `ref_table = build_ref_table(tree, alias_map)` (was removed by refine-and-confirm as unused — now it IS used). Pass `ref_table=ref_table` to `detect_dynamic_exec`. Signature change is internal.

## Refined Criteria

- `m = __import__('os'); m.system('cmd')` emits EXEC-002
- `mod = importlib.import_module('subprocess'); mod.call(['ls'])` emits EXEC-002
- `m = __import__('os'); f = m.system` stores `f` as `func_ref` in ref_table (needed for Part D)
- EXEC-006 still fires for the `__import__()` call alongside EXEC-002

## Dependencies Discovered

- `build_ref_table` in `_ast_ref_tracker.py`: returns `dict[str, RefEntry]` — must be called in `analyze_python` and result passed to `detect_dynamic_exec`
- `RefEntry` dataclass: `kind` is `Literal['module', 'func_ref']`, `resolved` is `str`
- `_DANGEROUS_NAMES` in `_ast_detectors.py`: includes `eval`, `exec`, `system`, `popen`, `getattr`, `__import__` — Part C should use execution subset (exclude `getattr`, `__import__`)
- `_UNSAFE_EXEC_CALLS` in `_ast_detectors.py`: fully-qualified dangerous calls like `os.system`, `os.popen`
- `_build_scope_map` already imported in `_ast_dynamic_exec_detector.py` (line 21)
- `_scoped_lookup` already imported but typed for `dict[str, str]` — need ref_table variant
