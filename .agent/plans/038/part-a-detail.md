# Part a: Depth-1 inline chain detection and builtins.__import__

JIT part detail generated by `/run` before delegating to build-and-test.

## Implementation Steps

1. **Step**: Add `_detect_inline_import_chain` node-level detector to `_ast_detectors.py`
   - **Files**: `src/skill_scan/_ast_detectors.py`
   - **Notes**: Match `Call(func=Attribute(value=Call, attr=dangerous))` where inner call is `__import__` or `importlib.import_module` (via `get_call_name`). For dangerous attrs, use a subset of `_DANGEROUS_NAMES` excluding `getattr` and `__import__` (indirection names, not execution names) — or better, check `_UNSAFE_EXEC_CALLS` for the fully-qualified form and `_DANGEROUS_NAMES - {'getattr', '__import__'}` for the short form. Extract module name from inner call's first `Constant` arg for the description. Emit EXEC-002 CRITICAL. ~20-25 lines. File is at 312/350 (38 lines headroom).

2. **Step**: Extend `_detect_dynamic_imports` to catch `builtins.__import__()`
   - **Files**: `src/skill_scan/_ast_detectors.py`
   - **Notes**: `get_call_name` returns `"builtins.__import__"` for `builtins.__import__('os')`. Simply add `"builtins.__import__"` (and `"__builtins__.__import__"`) to the name check, OR add a branch: if `name.endswith('.__import__')` and the prefix is in `_DANGEROUS_BASES`, emit EXEC-006. The prefix approach is more robust. ~3-5 lines.

3. **Step**: Register new detector and re-export from `ast_analyzer.py`
   - **Files**: `src/skill_scan/ast_analyzer.py`
   - **Notes**: Add `_detect_inline_import_chain` to `_DETECTORS` tuple (line 127) and re-export block (line 94). `ast_analyzer.py` at 139/350, ample room.

## Refined Criteria

- `__import__('os').system('cmd')` produces at least one EXEC-002 finding
- `__import__('os').popen('id')` produces at least one EXEC-002 finding
- `__import__('builtins').exec('code')` produces at least one EXEC-002 finding
- `importlib.import_module('os').system('cmd')` produces at least one EXEC-002 finding
- `builtins.__import__('os')` produces EXEC-006 finding
- Existing `__import__('os')` still produces EXEC-006 (no regression)

## Dependencies Discovered

- `get_call_name` in `_ast_imports.py`: resolves call names; returns `"builtins.__import__"` for the attribute form
- `_DANGEROUS_NAMES` in `_ast_detectors.py`: contains execution and indirection names (need subset for inline chain)
- `_UNSAFE_EXEC_CALLS` in `_ast_detectors.py`: fully-qualified dangerous execution calls
- `_DANGEROUS_BASES` in `_ast_detectors.py` (line 221): `{'builtins', '__builtins__'}` — reuse for builtins.__import__ check
- `_make_finding` helper: use for all new findings (hardcodes `category='malicious-code'`)
