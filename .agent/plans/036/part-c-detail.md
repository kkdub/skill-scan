# Part C: Integration tests for terminal branch exclusion

JIT part detail generated by `/run` before delegating to build-and-test. This is the final part.

## Implementation Steps

1. **Add `TestTerminalBranchExclusion` class to `test_int_list_branch_merge.py`**
   - **Files**: `tests/unit/test_int_list_branch_merge.py`
   - **Notes**: Use existing `_collect(code)` helper (line 16) which does `_collect_int_list_assigns(ast.parse(textwrap.dedent(code)))`. Tests exercise the full analyze path (ast.parse -> _collect_int_list_assigns -> _walk_fn_body) confirming end-to-end terminal branch exclusion. Wrap test code in function defs so `return`/`raise` are valid Python syntax. File is 209 lines with 91 lines free — plan criteria say stay under 300.

## Refined Criteria

- `if cond: codes=[...]; return` — excluded from merge, post-if uses else-path state
- `if cond: raise` — same behavior as return
- `if cond: codes=[...]; break` — NOT excluded (mutations visible after loop)
- Nested if where all inner paths return/raise — outer branch is terminal
- All branches terminal — state equals pre-branch snapshot (R004)
- Non-terminal branch preserved correctly (no regression vs PLAN-035 tests)
- Match case with terminal body excluded from merge
- global/nonlocal variable in terminal branch — scope routing still correct
- `test_int_list_branch_merge.py` stays at or under 300 lines
- `make check` passes

## Dependencies Discovered

- `_collect()` helper at line 16 of `test_int_list_branch_merge.py` — parses code and runs `_collect_int_list_assigns`
- `_SHADOW` imported from `_ast_split_int_list_tracker` at line 12
- Existing test classes (TestIfBranchMerge, TestMatchBranchMerge, etc.) serve as pattern reference
- Part B unit tests in `test_terminal_branch_exclusion.py` already confirm low-level behavior; Part C tests confirm end-to-end through the full collect pipeline
