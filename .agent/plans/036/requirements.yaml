- id: R001
  description: "_is_terminal_body(body) returns True only when body always exits scope (return, raise); break/continue return False (not scope-exit)"
  status: done
  verify: "test::tests/unit/test_terminal_body.py"

- id: R002
  description: "Recursive: nested if/else where all paths terminate counts as terminal; bare if (no else) does not"
  status: done
  verify: "test::tests/unit/test_terminal_body.py"

- id: R003
  description: "_walk_fn_body excludes terminal branches from _merge_branches for both If and Match"
  status: done
  verify: "test::tests/unit/test_int_list_branch_merge.py::TestTerminalBranchExclusion AND test::tests/unit/test_terminal_branch_exclusion.py"

- id: R004
  description: "When all branches are terminal, post-branch state equals the pre-branch snapshot"
  status: done
  verify: "test::tests/unit/test_terminal_branch_exclusion.py::test_both_terminal_result_equals_snapshot"

- id: R005
  description: "Conservative default: unrecognized or ambiguous structure is non-terminal (never lose detection)"
  status: done
  verify: "test::tests/unit/test_terminal_body.py"

- id: R-IMP001
  description: "New helper lives in a sibling module (_ast_terminal_body.py), not inlined in tracker"
  status: done
  verify: "grep::_ast_terminal_body"

- id: R-IMP002
  description: "Only import + call site added to _ast_split_int_list_tracker.py (minimal line increase)"
  status: done
  verify: "inspect"
