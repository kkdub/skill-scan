# Part B: Scanner-level precision — dedup preference and end-to-end validation

JIT part detail generated by `/run` before delegating to build-and-test.

## Implementation Steps

1. **Change _deduplicate() to prefer AST findings over regex**
   - **Files**: `src/skill_scan/content_scanner.py`
   - **Notes**: Current implementation at lines 199-215 seeds `seen` from regex, drops AST duplicates. New: build AST lookup dict by (rule_id, line), iterate regex findings replacing with AST version when available, then add remaining AST-only findings. This ensures AST precision (resolved names, correct severity) reaches scanner output while regex findings remain for lines AST doesn't cover. File at 239/350 — ample room.

2. **Create end-to-end scanner tests**
   - **Files**: `tests/unit/test_dynamic_exec_scanner.py`
   - **Notes**: Test at _apply_rules level (full regex+AST+dedup pipeline). Cases:
     - getattr(os, var='system') -> finding has 'system' in matched_text (AST precision)
     - getattr(os, unknown) -> finding severity is MEDIUM (not regex HIGH)
     - getattr(config, 'debug') -> finding is still HIGH (regex only, AST produces nothing for non-dangerous constant attr)
     - getattr(os, 'system') -> finding source is AST (preferred over regex)
     - getattr(self, 'process') -> finding is regex HIGH (AST doesn't flag benign self attrs)

## Refined Criteria

- getattr(os, var='system') scanner finding has 'system' in matched_text (AST preferred)
- getattr(os, unknown_param) scanner finding severity is MEDIUM (AST taint sink preferred over regex HIGH)
- getattr(config, 'debug') scanner finding is still HIGH (regex, no AST finding)
- getattr(os, 'system') scanner finding comes from AST (preferred over regex)
- No regression in existing test suite
- content_scanner.py stays at or under 350 lines
- make check passes

## Dependencies Discovered

- Part A's `detect_dynamic_exec` producing correct AST findings (EXEC-006 HIGH for resolved dangerous names, EXEC-006 MEDIUM for taint sinks)
- `_apply_rules` at content_scanner.py:~185 is the integration point
- `_deduplicate` at content_scanner.py:199 is the function to modify
- Existing tests in test_scanner.py exercise the general scanning pipeline

## Cross-part context from Part A

- detect_dynamic_exec is wired into analyze_python() and returns findings with category='malicious-code'
- AST findings use EXEC-006 rule_id (same as regex), so dedup by (rule_id, line) will match
- For getattr(os, var) where var='system', AST finding has 'system' in matched_text; regex finding just has the raw line
- For getattr(os, unknown), AST finding is MEDIUM severity; regex finding is HIGH
