# Part A: Tree-level dynamic exec detector (symbol table + taint sink)

JIT part detail generated by `/run` before delegating to build-and-test.

## Implementation Steps

1. **Add _SENSITIVE_MODULES and _resolve_first_arg to _ast_detectors.py**
   - **Files**: `src/skill_scan/_ast_detectors.py`
   - **Notes**: `_SENSITIVE_MODULES = frozenset({"os", "sys", "subprocess", "shutil", "socket", "builtins", "__builtins__", "importlib", "ctypes", "code", "codeop"})`. `_resolve_first_arg(node, alias_map)` resolves 1st arg of getattr to module name via alias_map. ~10 lines, file is at 294/350.

2. **Create _ast_dynamic_exec_detector.py with detect_dynamic_exec()**
   - **Files**: `src/skill_scan/_ast_dynamic_exec_detector.py`
   - **Notes**: Tree-level detector following detect_split_evasion pattern. Import `_build_scope_map` from `_ast_split_detector`, `_scoped_lookup` from `_ast_split_format`, `_DANGEROUS_NAMES` and `_SENSITIVE_MODULES` from `_ast_detectors`. Walk getattr Call nodes: (1) symbol-table resolution for 2nd arg Name -> HIGH if dangerous, (2) taint sink if 1st arg is sensitive module + 2nd arg non-resolvable -> MEDIUM, (3) skip Constant 2nd args. Use `Finding()` directly with `category='malicious-code'`.

3. **Wire detect_dynamic_exec into analyze_python()**
   - **Files**: `src/skill_scan/ast_analyzer.py`
   - **Notes**: Add import and call after `detect_kwargs_unpacking`. Pass `tree, file_path, alias_map, symbol_table, _nodes=all_nodes`.

4. **Create tests for symbol-table and taint-sink detection**
   - **Files**: `tests/unit/test_dynamic_exec_detector.py`
   - **Notes**: Test at analyze_python level. Cases: resolved var->system (HIGH), var->path (none), unknown var on os (MEDIUM taint), non-sensitive module (none), aliased module, self (none), hasattr (none), constant arg (not this detector).

## Refined Criteria

- getattr(os, var) where var='system' detected as EXEC-006 HIGH with 'system' in matched_text
- getattr(os, var) where var='path' does NOT produce a finding from detect_dynamic_exec
- getattr(os, unknown_var) detected as EXEC-006 MEDIUM (taint sink)
- getattr(config, unknown_var) does NOT produce a finding (non-sensitive module)
- Aliased modules resolve: import os as o; getattr(o, var) where var='system' detected
- getattr(self, unknown) does NOT produce a finding
- hasattr(os, 'system') does NOT produce a finding
- detect_dynamic_exec is called from analyze_python() and findings are included
- _ast_dynamic_exec_detector.py stays at or under 350 lines
- _ast_detectors.py stays at or under 350 lines
- make check passes

## Dependencies Discovered

- `_build_scope_map`: `src/skill_scan/_ast_split_detector.py:121`
- `_scoped_lookup`: `src/skill_scan/_ast_split_format.py:17`
- `_DANGEROUS_NAMES`: `src/skill_scan/_ast_detectors.py:22`
- `try_resolve_string`: `src/skill_scan/_ast_imports.py` (used by existing `_detect_dynamic_access`)
- `get_call_name`: `src/skill_scan/_ast_imports.py` (used by existing `_detect_dynamic_access`)
