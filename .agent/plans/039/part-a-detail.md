# Part A: TOML rule entry + detector module + registration

JIT part detail generated by `/run` before delegating to build-and-test.

## Implementation Steps

1. **Add EXEC-011 rule entry to malicious_code.toml**
   - **Files**: `src/skill_scan/rules/data/malicious_code.toml`
   - **Notes**: Add after EXEC-010 (line 220). Use `patterns = []` (AST-only, same as OBFS-001). severity=high, category=malicious-code. Include description and recommendation fields.

2. **Create `_ast_dunder_chain_detector.py` with `_detect_dunder_chain`**
   - **Files**: `src/skill_scan/_ast_dunder_chain_detector.py`
   - **Notes**: Node-level detector. Signature: `(node, file_path, *, alias_map=None) -> list[Finding]`. Detection: match `ast.Attribute` where `attr` is in dangerous dunder set, walk `.value` chain collecting consecutive dunders. Skip through `ast.Subscript` (recurse `.value`) and `ast.Call` (recurse `.func`). Severity: CRITICAL if chain contains any EXEC_ESCAPE_DUNDERS (`__globals__`, `__builtins__`, `__import__`, `__getattr__`, `__code__`), HIGH if 2+ MRO_WALK_DUNDERS (`__class__`, `__base__`, `__bases__`, `__mro__`, `__subclasses__`). Use `Finding()` directly (NOT `_make_finding` — `_ast_detectors.py` is frozen). Category: `malicious-code`.

3. **Register in ast_analyzer.py**
   - **Files**: `src/skill_scan/ast_analyzer.py`
   - **Notes**: Currently 153 lines. Add import at top (after existing `_ast_` imports). Add `_detect_dunder_chain` to `_DETECTORS` tuple (line 140-152). Add re-export in the facade re-export block.

4. **Update test_rule_validation.py**
   - **Files**: `tests/unit/test_rule_validation.py`
   - **Notes**: Add `"EXEC-011"` to `_AST_ONLY_RULES` frozenset (line 31, currently has `{"OBFS-001", "EXFIL-008"}`). Add `"EXEC-011"` to `ast_rule_ids` set (line 103, currently has `{"EXEC-002", "EXEC-006", "EXEC-007", "OBFS-001", "EXFIL-008"}`).

5. **Regenerate RULES.md**
   - **Files**: `RULES.md`
   - **Notes**: Run `make rules-catalog`. test_rules_md_is_fresh enforces exact parity.

## Refined Criteria

- EXEC-011 entry exists in malicious_code.toml with `patterns=[]`, `severity="high"`, `category="malicious-code"`
- `_ast_dunder_chain_detector.py` exports `_detect_dunder_chain` with signature `(node, file_path, *, alias_map=None) -> list[Finding]`
- `_detect_dunder_chain` is registered in `_DETECTORS` tuple in `ast_analyzer.py`
- `_detect_dunder_chain` is re-exported from `ast_analyzer.py` facade
- Chain of `().__class__.__base__.__subclasses__()` produces EXEC-011 HIGH finding
- Chain reaching `__globals__` or `__builtins__` produces EXEC-011 CRITICAL finding
- Isolated `obj.__class__` does NOT produce a finding (single dunder, not a chain)
- Isolated `cls.__subclasses__()` does NOT produce a finding
- Subscript nodes in mid-chain (e.g., `__mro__[1].__subclasses__()`) do not break detection
- Module is under 350 lines
- `_AST_ONLY_RULES` and `ast_rule_ids` in test_rule_validation.py include EXEC-011
- RULES.md is fresh (regenerated)

## Dependencies Discovered

- `Finding` and `Severity` from `skill_scan.models` — used for constructing findings
- `_DETECTORS` tuple in `ast_analyzer.py` — registration point for node-level detectors
- `_AST_ONLY_RULES` in `test_rule_validation.py` line 31 — exempt from "must have patterns" check
- `ast_rule_ids` in `test_rule_validation.py` line 103 — AST-emitted rule IDs for catalog coverage check
